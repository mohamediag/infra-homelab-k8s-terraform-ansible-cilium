# ==============================================================================
# group_vars/workers.yml — Variables for Kubernetes worker nodes
# ==============================================================================

# Worker nodes join the cluster using the command generated by kubeadm on cp-01
# The join command is retrieved from cp-01's hostvars in the workers role.
control_plane_host: "cp-01"
